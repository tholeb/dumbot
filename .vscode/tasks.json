{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "npm",
            "script": "start",
            "problemMatcher": [
                "$eslint-stylish"
            ],
            "label": "npm: start",
            "detail": "NODE_ENV=devel node -r dotenv/config . --enableServer 1"
        }
    ]
}